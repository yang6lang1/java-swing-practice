/**
 * Filename: Queueable.java
 * Author: AL
 * Student Number:
 * Lab:
 * Date of creation: June 2012
 * Description: interface for objects of class type that are to be queued
 */

public interface Queueable{  
   
   public String toString( );
   // Postcondition: concatenate the value of the object's 
   //                data members into a string and return this string.
  
/*
   public int compareTo(Queueable otherObject);
//Description method that deced whether this object is equal to,
// > than or < than otherObject
//postcondition: returns 0 if both objects match(are equal)
//			returns 1 if this object > otherObject
//			returns -1 if this object < otherObject 
*/
} // end of interface Queueable